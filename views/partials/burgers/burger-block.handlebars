<li>
	{{burger_name}}

	<button class="btn btn-success change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour{{else}} Trash{{/if}}
	</button>

	<button class="btn btn-success delete-burger" data-id="{{this.id}}">Trash</button>
</li>
